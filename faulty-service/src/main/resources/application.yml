server:
  port: 9002

eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://localhost:9010/eureka/
  instance:
    preferIpAddress: true

spring:
  application:
    name: faulty-service