server:
  port: 9002

eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://${EUREKA_SERVER:localhost}:9010/eureka/
  instance:
    preferIpAddress: true

spring:
  application:
    name: faulty-service