server:
  port: 9001

eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://${EUREKA_SERVER:localhost}:9010/eureka/
  instance:
    preferIpAddress: true

spring:
  application:
    name: healthy-service
